procedure start;


procedure start
begin
    if (script_action == use_proc) then begin
        script_overrides;
        display_msg("The elevator is broken. Maybe you can use the ladder.");
    end
end
