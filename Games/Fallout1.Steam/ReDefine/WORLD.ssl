procedure start;


procedure start
begin
    if (script_action == spatial_proc) then begin
        if (source_obj == dude_obj) then begin
            script_overrides;
            display_msg("You are now entering the world map.");
            world_map;
        end
    end
end
